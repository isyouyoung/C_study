#include <string.h>
#include <stdio.h>

void main()
{
	char ss[] = "XYZ";
	int len;

	len = strlen(ss);

	printf("문자열 \"%s\"의 길이 ==> %d \n", ss, len);
	printf("\n큰따옴표의 내용을 출력하기 위해 \"문자를 사용함\n");

	printf("\n공부중=>코드 분석중");
}
